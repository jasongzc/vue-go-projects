{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/backend",
            // "args": [], // 如果你的程序需要命令行参数
            // "env": {},  // 如果你需要设置环境变量
            "cwd": "${workspaceFolder}" // Current Working Directory (当前工作目录)
        }
    ]
}